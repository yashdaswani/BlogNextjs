/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com', 'otherdomain.com'], // Add your trusted image domains here
      }
}

module.exports = nextConfig
